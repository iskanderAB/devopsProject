apiVersion: v1
kind: Service
metadata:
  name: conduit-backend
  labels:
    app: conduit-backend
spec:
  type: NodePort
  selector:
    app: conduit-backend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30030
